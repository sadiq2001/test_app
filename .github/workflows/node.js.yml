name: Node.js CI/CD Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '20'

    - name: Install dependencies
      run: |
        npm install

    - name: Run tests
      run: |
        npm test

    - name: Start server
      run: |
        npm start &
        sleep 10 
    - name: Deploy to Server via SSH
      if: success()
      uses: appleboy/ssh-action@v0.1.1
      with:
        host: ${{ secrets.SERVER_IP }}   # Add your server IP as a secret
        username: ubuntu                 # Your server username (e.g., ubuntu)
        key: ${{ secrets.DEPLOY_SSH_KEY }}  # The SSH key you just added
        script: |
          cd downloads/simple_nodeapp    # Navigate to your app directory on the server
          git pull origin main           # Pull the latest changes
          pm2 restart simple_nodeapp
